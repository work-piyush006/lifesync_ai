name: LifeSync AI Alarm Build CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Flutter
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'

      # 3️⃣ Cache Flutter dependencies
      - name: Cache Flutter Pub
        uses: actions/cache@v3
        with:
          path: |
            ~/.pub-cache
            .packages
          key: ${{ runner.os }}-flutter-${{ hashFiles('pubspec.yaml') }}

      # 4️⃣ Ensure Android folder exists (generate missing files)
      - name: Ensure Android folder
        run: |
          if [ ! -f "android/app/build.gradle" ] || [ ! -f "android/settings.gradle" ]; then
            echo "⚠️ Android folder incomplete. Generating..."
            flutter create . --platforms android
          else
            echo "✅ Android base files found"
          fi

      # 5️⃣ Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 6️⃣ Lock critical plugins versions
      - name: Lock plugin versions
        run: |
          flutter pub add geolocator:^14.2.0
          flutter pub add audioplayers:^6.1.0
          flutter pub add flutter_plugin_android_lifecycle:^2.0.26
          flutter pub get

      # 7️⃣ Set Android SDK versions
      - name: Set Android SDK versions
        run: |
          sed -i 's/compileSdkVersion .*/compileSdkVersion 35/' android/app/build.gradle || true
          sed -i 's/targetSdkVersion .*/targetSdkVersion 35/' android/app/build.gradle || true
          sed -i 's/minSdkVersion .*/minSdkVersion 21/' android/app/build.gradle || true
          echo "✅ SDK versions set"

      # 8️⃣ Force Gradle version
      - name: Force Gradle version
        run: |
          if [ -f "android/gradle/wrapper/gradle-wrapper.properties" ]; then
            sed -i 's|distributionUrl=.*|distributionUrl=https\://services.gradle.org/distributions/gradle-8.6-all.zip|' android/gradle/wrapper/gradle-wrapper.properties
          fi

      # 9️⃣ Generate local.properties
      - name: Generate local.properties
        run: |
          echo "flutter.sdk=/opt/hostedtoolcache/flutter/stable-3.24.3-x64" > android/local.properties
          echo "sdk.dir=/usr/lib/android-sdk" >> android/local.properties
          echo "✅ local.properties created"

      # 🔟 Check launcher icon
      - name: Check launcher icon
        run: |
          if [ ! -f "assets/logo.png" ]; then
            echo "❌ assets/logo.png not found!"
            exit 1
          else
            echo "✅ Launcher icon found"
          fi

      # 1️⃣1️⃣ Generate Launcher Icons
      - name: Generate Launcher Icons
        run: flutter pub run flutter_launcher_icons

      # 1️⃣2️⃣ Clean Flutter project
      - name: Clean Flutter project
        run: flutter clean

      # 1️⃣3️⃣ Get dependencies again
      - name: Get Flutter dependencies
        run: flutter pub get

      # 1️⃣4️⃣ Build Release APK
      - name: Build Release APK
        run: flutter build apk --release --no-shrink

      # 1️⃣5️⃣ Upload Release APK
      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
